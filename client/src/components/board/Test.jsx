{
  /* <table className="mt-10">
<tbody>
  {board.map((column, i) => (
    <tr key={i}>
      {column.map((row, j) => (
        <td
          key={`${i}-${j}`}
          className={`h-[35px] w-[35px] border border-[#7c7c7c] ${
            row > 0 ? "bg-blue-500" : row < 0 ? "bg-red-500" : ""
          }`}
        >
          {row}
        </td>
      ))}
    </tr>
  ))}
</tbody>
</table> */
}
